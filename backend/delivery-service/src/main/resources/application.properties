# Application
spring.application.name=delivery-service

# Server
server.port=8087

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/ordereasy_delivery_db
spring.datasource.username=spring_user
spring.datasource.password=Spring@2024

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Kafka
spring.kafka.bootstrap-servers=localhost:9092

# Consumer (Delivery Service will consume order-created)

spring.kafka.consumer.group-id=delivery-group
spring.kafka.consumer.auto-offset-reset=earliest

spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer

spring.kafka.consumer.properties.spring.json.trusted.packages=*
spring.kafka.consumer.properties.spring.json.value.default.type=com.ordereasy.delivery_service.event.OrderCreatedEvent